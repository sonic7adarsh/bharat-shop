# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: 'BharatShop Dashboards'
    orgId: 1
    folder: 'BharatShop'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards